# 里程碑

原则：**每个里程碑结束时，应用必须可运行、可验证、可交付**，不做"半成品堆叠"。

| 里程碑 | 范围 | 可验证的交付 | 状态 |
| --- | --- | --- | --- |
| **M1 最小可用闭环** | 打开 Vault、文件树（虚拟化）、编辑 Markdown、原子保存、基础预览、冲突处理、主题/命令扩展点、回收站删除 | `pnpm tauri:dev` 起应用 → 打开 Vault → 建笔记 → 编辑保存（状态栏显示耗时）→ 预览同步 → 删除进 `.mimenote/trash` | ✅ 已交付 |
| **M1.5 测试与自动化** | 两层 E2E（真实二进制 / 真实 Chromium）、命令行打开 Vault、日志落盘、布局回归门禁 | `pnpm test:e2e` 全绿；`mimenote.exe <vault>` 可直接打开 | ✅ 已交付（提前于 M5） |
| **M2 核心体验** | wikilink 双链、反向链接、重命名（含链接更新）、标签、Frontmatter、全文搜索（SQLite FTS5）、快速切换、命令面板 UI、`asset:` 图片渲染 | ✅ **已交付**：双链 + 反向链接 + 悬空链接一键创建；重命名（同目录改名 + 全库链接精确改写 + `F2` 对话框）；标签与 Frontmatter（`mn-core` 抽取 + 索引 + `Ctrl+Shift+T` 面板）；全文搜索（FTS5 + `bm25` + 第三个面板模式 + `Ctrl+Shift+F`）；快速切换与命令面板（`Ctrl+P` / `Ctrl+K`）；本地图片（asset 协议**逐文件授权**，见 ADR-0007）。当时推迟的三项：**跨目录移动**与**命中行跳转**已在 M3 交付，**标签重命名/合并**仍未做 |
| **M3 高级功能** | 图谱视图、**所见即所得编辑**、**设置页与菜单**、**本地图片完善**、工作区布局持久化、**多标签页**、**导出 HTML/PDF**、拖拽整理文件 | 布局重启后恢复、导出产物可在浏览器打开 | ✅ **已交付**：**知识图谱卡片画布**（`Ctrl+G`，见 ADR-0010，并随编辑自动刷新且保留视角）；**所见即所得编辑**（Live Preview，**移除双栏**，见 ADR-0009）；**设置页 + 应用菜单**（`Ctrl+,`）；**图片**支持 `![[嵌入]]`、裸文件名全库解析、点击放大灯箱，以及**粘贴/拖入自动落到附件目录**（ADR-0013）；**工作区布局持久化**（视图模式/侧栏/面板宽度/标签面板与大纲面板开关/目录展开/卡片位置）；**多标签页**（标签栏 + 切换/关闭/持久化 + `Ctrl+1..9`）；**导出**（自包含 HTML + 打印/另存为 PDF，ADR-0011）；**拖拽整理文件**（`note_move`：三种落点、悬停反馈、原子搬迁、全库链接改写、同名不覆盖，`F6` / 工具栏按钮 / 命令面板是键盘入口；**移动时同时改写被移动笔记自身正文里的相对链接**，ADR-0012）。M3 之后追加交付的体验项：**搜索命中行跳转**（含反向链接）、**大纲面板**（`Ctrl+Shift+O`，含"当前章节"高亮）、**阅读视图代码块复制**、**窗口标题跟随当前笔记**、**图片尺寸语法**（`![[图.png|300]]`）、**阅读视图字号**（设置页独立一项）、**Markdown 表格对齐格式化**（`Mod+Alt+F`，走命令表因此命令面板可搜到）、**URL 粘到选中的文字上自动包成 Markdown 链接**（由 `@codemirror/lang-markdown` 的 `pasteURLAsLink` 提供，默认开启 —— 我们曾手写等价实现，发现两份会互相抢先后删掉自写版，改为用测试钉住上游行为）。**目录重命名 / 目录移动也已交付**（ADR-0015：整棵子树的路径与全库链接一起改，复用单篇搬迁链路 + 前缀映射；1000 篇实测 2–10 s，与被引用篇数成正比）。**标签的三件事都补齐了**：面板里**加/删**（ADR-0006「后续修订」）、**全库重命名/合并**（连带正文行内 `#标签`、跳过代码块与行内代码、如实汇报跳过清单）、**文件树按标签过滤**（只读收窄：祖先目录保留、多选并集、「含子标签」显式开关、失败则整轮不收窄）。**表格在所见即所得里也渲染了**（ADR-0009 的后续补齐：整块露原文、列多时横向滚动、单元格内联语法照常渲染），**回收站恢复也接上了宿主命令与界面**（ADR-0018：恢复原位置 / 「恢复为…」、绝不覆盖占位者、孤儿记录如实标出），**标签的层级编辑也补齐了**（ADR-0006 第三条后续修订：全库概览里的 `⇥`「移到…」→ 挂到父标签下 / 留空提回顶层；`tag_move` 与重命名共用同一条写路径与同一个对话框，**目标被占用时拒绝**并指向「重命名」，非法移动在写盘前拦下且理由原话显示），**整库导出静态站点也交付了**（ADR-0019：每篇一个 HTML + 双链变成相对链接 + 图片复制进 `assets/` + 整站一份样式表 + 索引页，**零 JavaScript**；输出目录必须在 Vault 之外、非空目录凭标记认领、从不删除、页面里不含时间戳所以两次导出逐字节相同）。仍推迟：回收站的**永久删除/清空**、标签面板上的**拖拽成树**、静态站点的**站内搜索与增量重导出**。**图谱按用户反馈重做了一轮**（ADR-0021）：默认进入的是**关系图**——以当前打开的笔记为圆心、深度可调 1..5（双向）、每张卡片正面就是那篇笔记的**完整 Markdown 预览**、卡片改由 **canvas** 绘制（80 张全在屏幕里的一帧 1.1 ms；同样这 80 篇走一遍阅读视图渲染管线是 102.8 ms，且不含 DOM 落地与排版），"整个 Vault"视图保留为可切换的另一种视图（文件夹成组、卡片可拖动、入链虚线出链实线、悬空链接点名）。**Obsidian 风格 callout 也交付了**（ADR-0022：`> [!note] 标题`，13 种类型 + 别名 + 折叠符 + 未知类型退化，在阅读视图/所见即所得/导出件/静态站点/图谱卡片**五处**一致；判据只有 `domain/callouts.ts` 一份，强调色只有 app.css 一份；编辑器里 `[!note]-` 真的收起且点图标能把 `-` ↔ `+` 写回文档）。**图谱的第二轮（Obsidian 化）也交付了**（ADR-0023：环布局给种子 + **确定性力导向**松弛，张力/斥力/向心力/阻尼可调、五个预设，节点像漂浮一样被张力牵着走、按住一张它就不动而其余继续漂；**连线从正文里 `[[链接]]` 那段文字用虚线引出**、出了卡片才变实线/张力曲线，找不到文字时如实降级；**卡片大小可调**（宽度 + 高度上限档位，按 Vault 持久化）；**浮动笔记面板**（可拖动/缩放/多个/置顶，`Esc` 先关浮窗）；顺带修掉"卡片预览里的列表项之间多出一条段间距"）。**图谱按反馈又修了一轮**：① 力度管理面板 + **碰撞是硬约束**（"图谱应该有力度管理，笔记之间应该有碰撞！"）；② **拖拽跟手**（"拖拽卡片没有变动位置，然后没有碰撞推动卡片，整体非常僵硬！" —— 根因是 `pins` 曾是"创建模拟"的依赖，每个 pointermove 都把卡片弹回原位；顺带修掉"命中测试用环上的原始矩形"这处会让用户抓错卡片的缺陷，并让交互 `heat` 重新加热力场、漂浮有一个很小的强度底值）；③ **卡片内的虚线引线单独成层**（"从 wiki 链接处虚线开始，卡片边缘处实线出连接到卡片" —— 几何本来就是对的，坏的是那段引线被不透明的 canvas 卡片整段盖住，真机上看起来"线从卡片边缘凭空开始"；同时把虚线相位锚在边界那一端，消掉真实数据里 0.7~2.4px 的缝）。**任务列表也补齐了**（ADR-0024：`- [ ]` / `- [x]` 在阅读视图 / 导出件 / 应用内打印 / 静态站点 / 图谱卡片五处一致地渲染成**禁用的原生复选框**，判据收敛成 `domain/task-list.ts` 一份 —— 兑现了 `blocks.ts` 里"将来两边的判据应当合并成一份"那句注释），**所见即所得的列表标记也真的渲染出来了**（无序画 `•`/`◦`/`▪`，有序按列表起始值算序号 —— `1. 1. 1.` 显示成 `1. 2. 3.`）。 |
| **M3 之后（这一轮）** | 图谱卡片与连线的两条议题（ADR-0025 / 0028）、界面模块化（ADR-0026）、界面偏好与阅读体验（ADR-0027） | ✅ **已交付**：① **图谱卡片**——frontmatter 不再画在卡片上（判据仍是 `domain/frontmatter.ts`）、**「仅标题」档**（一屏看更多关系，引线如实降级）、缩放手柄**同时调宽高** + 高度档新增**「全文」**（不截断，与数值档互斥）、**引线加粗加深但仍是虚线**（用户反馈"太淡了"）、**悬停正文里的 `[[链接]]` 提亮对应的连线**（`linkZones` 与 `findLinkAnchor` 共用同一套遍历）、**失去焦点即松开**（点空白 + 选中变化两条路都加热力场）、**移除停靠预览面板**（单击 = 选中，"读全文"交给浮窗）；顺带修掉"焦点视图下卡片尺寸从不落盘"（`rootPath` 过去只由全库视图写入）；② **连线重做**（用户建议 + 讨论定稿）——**语义色相**（暖 = 我指向它、冷 = 它指向我、中性 = 环与环之间；令牌 `--mn-edge-out/--mn-edge-in` 可选、不进 `REQUIRED_TOKENS`）、**跳数编码粗细与透明度**（淡化/强调在基准上**乘性调制**，否则跳数权重永远看不见）、**环向走线**（同环沿"最远的角之外"的弧走、跨环朝外鼓、涉及圆心保持 ADR-0023 原样；换形状时锚点换到卡片外缘、引线随之重画以守住分界纪律），HUD 上「沿环走线」可关；③ **视图模块停靠**——四块面板可拖到左/右/底三区并在区内重排（空区在拖动期间出现"能放"轨道），键盘 `Alt+1/2/3` 与同区 `Alt+方向键`，可见性仍归各开关（四条切换快捷键未改）；**标签栏移到窗口顶部横跨全宽**（删掉 `.mn-main:has(> .mn-tabs)`）；④ **通用右键菜单**接入文件树行 / 标签页 / 停靠模块头 / 图谱卡片（菜单项直接执行既有命令，`tabs-store` 新增 `closeOthers`/`closeAll`）；⑤ **文件树排序可配置**（名称/修改时间/大小/类型 × 升降 × 目录在前，判据只有 `domain/tree.ts` 一份）、**最近打开的 Vault**（左下角、最多 8 条、可移除）、**阅读主题「纸墨」**、**已完成任务加删除线**（阅读视图/导出件/静态站点/所见即所得；编辑器里是只圈文字的 mark 装饰）。下一轮明确留着的连线工作：**把连线搬进 canvas**（60fps 与所有线动画的前置）、渐细笔触取代箭头、同一对笔记的多条链接画成平行束、真正的避障布线。**这一轮（修补轮）**：**标题栏定型为左/中/右三区**（ADR-0029）—— 左 = 哪个库、中 = 哪一篇（**当前笔记路径**）、右 = 多大 + 出口动作；路径从编辑器面板搬进中区，顶部高度因此恒定 34px、**阅读与图谱视图里也看得见当前是哪一篇**，编辑器里那行 26px 的路径栏整条删掉（中区落正中靠的是左右两条等宽网格轨道 `1fr / 2fr / 1fr`）；**标签栏移到标题栏上方**（窗口最顶行给标签，空白段仍是拖动区）、**附件只读查看器**（ADR-0032：文件树里点图片能预览，走 `asset:` 逐文件授权、只读、不进标签页；判据只有 `domain/viewable.ts` 一份 —— JSON/TOML/CSV 挂同一根主轴）、**VI 落地第一批**（ADR-0031：设计令牌两层命名 + 别名层、**默认字号三档统一 16px**、文件树行高 26→30、新增"四栏不裁字 + 树行留白 ≥3px"门禁）；**界面上不写笔记的 `.md`**（ADR-0030）—— 判据只有 `domain/paths.ts` 的 `displayName` / `displayPath` 一份，只在"这是哪一篇"的**标识显示**上生效（标签页 / 标题栏 / 文件树 / 反链出链 / 快速切换 / 搜索命中 / 窗口标题 / 回收站），而**导出件与静态站点、宿主报错原文、移动对话框这类"编辑路径"的地方保留真实文件名**；悬停 `title` 与 `data-note-path` / `data-tab-path` / `data-rel-path` 一律给真实路径（自动化的身份探针不再读可见文字） |
| **M4 插件系统** | manifest（id/name/version/minAppVersion/permissions）、版本化 API、Worker 隔离、权限确认 UI、错误边界、卸载清理 | 示例插件在权限最小化前提下加载并卸载无残留 | 待开始 |
| **M5 性能与稳定性** | 增量索引、缓存、Web Worker 预览、基准测试套件 + CI 阈值、E2E 扩到更多场景 | `pnpm bench` 输出与 §6 预算对比；E2E 覆盖 M1 闭环 | 待开始（**E2E 底座已提前交付**）。**增量索引已提前交付**：缓存库按 `(path, mtime_ms, size)` 跨会话复用，**链接 / 标签 / 全文搜索三者共用同一份判定键**（打开 1 万笔记 Vault：搜索这一路 4.7–5.0 s → 0.17–0.23 s，见 ADR-0008「后续修订」；链接 + 标签 4.9–5.1 s → 对账 + 装载 0.21–0.28 s，整轮「打开 Vault」9.7–9.9 s → **0.25–0.34 s**，见 ADR-0014）。**外部改动自动同步也已提前交付**（`notify` 8 只加在宿主 crate，见 ADR-0016）：宿主监听整棵 Vault 目录 → 去抖 500 ms / 硬上限 2 s → 拿磁盘 `(mtime, size)` 与条目表对账，判为**外部**改动才推 `mn://vault-changed`，前端静默重扫并让当前笔记跟随磁盘（1 万笔记实测：判定 502–510 ms + 重扫 107 ms + 复用增量构建 105 ms ≈ **宿主侧 714 ms**，前端建树 11.7 ms；监听常驻开销 4 句柄 / 2 线程 / ≈0.5 MB 工作集，停掉即回基线）。**仍未做**：**按文件精细增量**（当前是"去抖合并成一次重扫 + 复用既有增量构建"，不是只重解析改动的那几篇）、内容哈希二级令牌（同一毫秒 + 同字节数的改动会漏判）、**大文档预览的分块 + 视口窗口化**（ADR-0020 已交付另外三件：图片授权不再触发整篇重渲染、wikilink 补类名线性化、**超过 1 MiB 的文档把 markdown-it 解析搬进 Web Worker**；净化与 DOM 落地经实测只能留在主线程，窗口化才是消灭十万级 DOM 元素的办法）、基准套件进 CI（本轮只有 `#[ignore]` 的手工基准：`cargo test -p mn-index --release -- --ignored --nocapture bench_reopen_10k_notes`、`--test persisted_index bench_open_vault_10k_notes`、`cargo test -p mimenote --release -- --ignored --nocapture bench_watch_latency_on_a_ten_thousand_note_vault`） |
| **M6 发布** | 打包（MSI/NSIS/dmg/AppImage）、签名、更新通道、文档、贡献指南 | 安装包在干净机器上安装运行 | 待开始 |

## M1 范围冻结（本次交付）

**做**：
- pnpm + Cargo 双 workspace，`mn-core` 纯逻辑库与 Tauri 应用分层
- `mn-core`：路径防护、原子写、迭代式 Vault 扫描、回收站、文本统计（含单元/集成测试）
- Tauri 应用：会话状态、12 个 IPC 命令、稳定错误码、CSP、能力声明最小化、日志落盘、命令行打开 Vault
- 前端：领域层（树/虚拟列表/EOL/Markdown 净化）、状态层、IPC 可替换适配器
- UI：Vault 门闸、虚拟化文件树（键盘导航 + 过滤）、CM6 编辑器（自动保存/冲突横幅）、实时预览、状态栏（字数/保存耗时/主题切换）、可拖拽分栏
- 内置扩展点：命令注册表 + 快捷键、JSON 主题（CSS 变量）、Vault CSS 片段
- 测试：Rust 单元/集成 + TS 领域层单测 + 组件集成（jsdom）+ **两层 E2E**（真实二进制 / 真实 Chromium）

**不做**（推迟，见 architecture.md §8）：双链/反链/标签/搜索/图谱/插件/同步/内嵌图片。
