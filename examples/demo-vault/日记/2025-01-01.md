# 2025-01-01

今天把 M1 的闭环跑通了。

## 做到的事

- 打开一个普通文件夹作为 Vault
- 文件树用虚拟列表渲染（一万条笔记也只挂载几十行 DOM）
- 编辑 Markdown，输入路径上没有任何同步 IO
- 保存走「临时文件 + fsync + rename」的原子替换
- 预览与编辑器分栏，可拖拽调整比例

## 没做的事

> 双链、标签、全文搜索、图谱都留到后面的里程碑。
> 先把地基打对，再往上盖。

1. 双链与反向链接
2. SQLite FTS5 全文搜索
3. 命令面板与快速切换
